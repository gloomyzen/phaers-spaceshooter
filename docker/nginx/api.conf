server {
    listen 443 ssl;
    index index.php index.html;
    # for most websites
    # root /var/www;
    # for laravel
    root /var/www/public;

    error_log  /var/www/storage/logs/error_nginx.log warn;

    ssl on;
    ssl_certificate /etc/nginx/ssl/ssl-cert-snakeoil.pem;
    ssl_certificate_key /etc/nginx/ssl/ssl-cert-snakeoil.key;

    #proxy_set_header X-Real-IP  $remote_addr;
    #proxy_set_header X-Forwarded-For $remote_addr;
    #proxy_set_header X-Forwarded-Proto $scheme;
    #proxy_set_header Host $host;

    client_max_body_size 20M;

    index index.html;

    location / {
        try_files $uri /index.php?$args;
    }

    location /docs {
        try_files $uri $uri/;
    }

    location ~ \.php$ {
        fastcgi_split_path_info ^(.+\.php)(/.+)$;
        fastcgi_pass php-fpm-api:9000;
        fastcgi_index index.php;
        include fastcgi_params;
        fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
        fastcgi_param PATH_INFO $fastcgi_path_info;
    }

    ## cache .appcache
    #location ~* \.(?:manifest|appcache|html?|xml|json)$ {
    #  expires 1M;
    #}

    #location ~* \.(?:png|jpe?g|gif|ico|hdr|gltf|bin|eot|svg|woff2?|ttf|css|js)$ {
    #    access_log off;
    #    gzip_static on;
    #    gzip_comp_level 5;
    #    #expires 24h;
    #    expires 1M;
    #    add_header Cache-Control "public";
    #    log_not_found off;
    #    try_files $uri $uri/ @production;
    #}

    #location @production {
    #    resolver 8.8.8.8;
    #    proxy_pass https://production.site/$uri;
    #}

}
